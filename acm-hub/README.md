# ACM Policies

Este diretório contém `Policy`/`Placement`/`PlacementBinding` do Red Hat Advanced Cluster
Management (RHACM) usadas para preparar automaticamente os clusters dos labs deste repositório.

```bash
oc apply -k policies/
```
