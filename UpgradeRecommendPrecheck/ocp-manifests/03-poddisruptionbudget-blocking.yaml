apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: nginx-pdb-demo-strict
  namespace: lab-upgrade-status
spec:
  minAvailable: 100%
  selector:
    matchLabels:
      app: nginx-pdb-demo
